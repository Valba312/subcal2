meta:
  environment: node
  toolchain:
    name: npm
    version: 22

build:
  additionalCommands: npm run build
  artifacts:
    "*": /
    ".next": /.next

run:
  command: npm start
  persistenceMount: /data
  containerPort: 10000
