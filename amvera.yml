meta:
  environment: node
  toolchain:
    name: npm
    version: 22

build:
  additionalCommands: npm run build
  artifacts:
    "*": /

run:
  command: npm start
  persistenceMount: /data
  containerPort: 10000
